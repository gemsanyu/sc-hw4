Contains:
trained_miner.py : reads in weights from trained neural network stored as pickle file. The default name is trained_miner.pkl
miner_harnessX.py : Three different files with fixed locations. Can be used with trainder_miner.py by changing the imports.

Score is now calculated by alive_time/4 + minerals*100

